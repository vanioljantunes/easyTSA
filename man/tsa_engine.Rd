% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{tsa_engine}
\alias{tsa_engine}
\title{Start (or reuse) the TSA calculation engine}
\usage{
tsa_engine(jar = NULL, restart = FALSE)
}
\arguments{
\item{jar}{Path to \code{TSA.jar} (default from \code{\link[=tsa_jar]{tsa_jar()}}).}

\item{restart}{Logical; discard the cached engine and reload the jar.}
}
\value{
Invisibly, an environment with \code{jar}, \code{java_version},
\code{tsa_version} and \code{io} (the program's file reader object).
}
\description{
Starts a headless Java virtual machine with the TSA program's jar on the
classpath and keeps it for the session. Called automatically by
\code{\link[=tsa_run]{tsa_run()}}; call it yourself to check the installation.
}
\examples{
\dontrun{
tsa_setup()   # once per machine
tsa_engine()
}
}
