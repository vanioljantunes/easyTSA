% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{tsa_setup}
\alias{tsa_setup}
\title{Install the TSA program (and Java) for easyTSA}
\usage{
tsa_setup(
  dir = tools::R_user_dir("easyTSA", "data"),
  java = TRUE,
  accept_license = NULL,
  force = FALSE,
  url = .tsa_url
)
}
\arguments{
\item{dir}{Folder to install the TSA program into.}

\item{java}{Logical; install a Java runtime when none works.}

\item{accept_license}{\code{TRUE} to accept the TSA license without a prompt
(required in non-interactive sessions), \code{FALSE} to decline, \code{NULL} to
ask.}

\item{force}{Logical; download again even when the program is found.}

\item{url}{Address of the TSA program zip.}
}
\value{
Invisibly, the saved configuration (\code{jar}, \code{java_home}).
}
\description{
Downloads the Copenhagen Trial Unit TSA program (0.9.5.10 Beta) from its
official address, unzips it into a user folder, installs a Java runtime
(Amazon Corretto, through \pkg{rJavaEnv}) when no working Java is found,
and remembers both locations so that \code{\link[=tsa_run]{tsa_run()}} finds them in every
later session. Run it once per machine.
}
\details{
The TSA program is free to use but its license does not allow
redistribution, which is why easyTSA downloads it from its authors
instead of bundling it. Before downloading you are asked to accept the
license; its full text is \code{license_agreement.pdf} in the installed
folder. easyTSA is not affiliated with or endorsed by the Copenhagen
Trial Unit.
}
\examples{
\dontrun{
tsa_setup()
}
}
\seealso{
\code{\link[=tsa_remove]{tsa_remove()}}, \code{\link[=tsa_engine]{tsa_engine()}}
}
