% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsa.R
\name{tsa_create}
\alias{tsa_create}
\title{Trial Sequential Analysis of a meta-analysis object}
\usage{
tsa_create(
  m,
  rrr = NULL,
  control = NULL,
  intervention = NULL,
  md = NULL,
  variance = NULL,
  alpha = 0.05,
  beta = 0.2,
  diversity = "D2",
  futility = TRUE,
  model = c("random", "common"),
  outcome = c("negative", "positive"),
  sortvar = NULL,
  spending = "obf",
  label.e = NULL,
  label.c = NULL,
  title = NULL,
  ngrid = 801
)
}
\arguments{
\item{m}{A \code{meta} object (\code{metabin} or \code{metacont}).}

\item{rrr}{Anticipated relative risk reduction (binary data), e.g. \code{0.20}.
Default \code{NULL} is the "empirical" option of the TSA software: the
event proportion of each arm is averaged over trials with the weights
of the meta-analysis model, and the RRR is \code{1 - p_e / p_c}. This is
generally optimistic; give a clinically minimal effect when you have
one.}

\item{control}{Anticipated control event proportion (binary data).
Default \code{NULL} uses the weight-averaged control-arm proportion of \code{m}.}

\item{intervention}{Anticipated intervention event proportion (binary
data); alternative to \code{rrr}. Default \code{NULL}.}

\item{md, variance}{Anticipated mean difference and variance (continuous
data). Defaults use the pooled estimate and the pooled within-trial
variance.}

\item{alpha}{Two-sided type I error (default 0.05).}

\item{beta}{Type II error (default 0.20 = 80 percent power).}

\item{diversity}{Heterogeneity adjustment of the RIS: \code{"D2"} (default,
diversity from \code{m}), \code{"I2"}, \code{"none"}, or a number in \verb{[0, 1)}.}

\item{futility}{Logical, compute the inner wedge (default \code{TRUE}).}

\item{model}{\code{"random"} (default, if \code{m} has a random-effects model) or
\code{"common"}; which pooled estimate feeds the Z-curve.}

\item{outcome}{\code{"negative"} (default; the event is a harm, so a relative
risk below 1 favours the intervention) or \code{"positive"}. Controls the
sign convention: positive Z always means "favours intervention".}

\item{sortvar}{Variable giving the trial order (usually publication
year). Default: \code{year} column of the data used in \code{m}, else the order
of \code{m}. Ties are broken alphabetically by study label (TSA program
convention).}

\item{spending}{Spending function for both boundaries, see
\code{\link[=tsa_spending]{tsa_spending()}}.}

\item{label.e, label.c}{Arm labels used in plots and files. Defaults
come from \code{m} (\code{label.e}, \code{label.c}) or are \code{"Intervention"} /
\code{"Control"}.}

\item{title}{Analysis title (default \code{m$outclab} or \code{"Meta-analysis"}).}

\item{ngrid}{Grid size for the boundary integration (see \code{\link[=tsa_bounds]{tsa_bounds()}}).}
}
\value{
An object of class \code{"easytsa"} (a list). Use \code{\link[=print]{print()}},
\code{\link[=summary]{summary()}}, \code{\link[=tsa_plot]{tsa_plot()}}, \code{\link[=tsa_write]{tsa_write()}}, \code{\link[=tsa_launch]{tsa_launch()}}.
}
\description{
Runs a Trial Sequential Analysis (TSA) on an object created by
\code{\link[meta:metabin]{meta::metabin()}} or \code{\link[meta:metacont]{meta::metacont()}}: cumulative Z-curve in trial
order, required information size (RIS) with heterogeneity adjustment,
Lan-DeMets O'Brien-Fleming monitoring boundaries, futility boundaries
(inner wedge) and the TSA-adjusted confidence interval.
}
\examples{
m <- meta::metabin(event.e, n.e, event.c, n.c, data = atb_peecs,
                   studlab = paste(author, year), sm = "RR",
                   method = "MH", method.tau = "REML",
                   random = TRUE, common = FALSE)
# empirical effect: weighted arm proportions from the meta output
x <- tsa_create(m, label.e = "ATB", label.c = "No ATB",
                title = "PEECS after ESD")
x
summary(x)
# clinically anticipated effect instead
tsa_create(m, rrr = 0.30)
}
