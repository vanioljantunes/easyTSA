% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{tsa_plot}
\alias{tsa_plot}
\title{Plot a Trial Sequential Analysis}
\usage{
tsa_plot(
  x,
  title = x$settings$title,
  subtitle = "auto",
  xlab = "Cumulative number of participants",
  ylab = "Cumulative Z-score",
  show_futility = x$settings$futility,
  show_conventional = TRUE,
  show_labels = FALSE,
  show_favours = TRUE,
  ris_label = "auto",
  col_z = "#1f4e9c",
  col_bound = "#c0392b",
  col_futility = "#c0392b",
  col_conv = "grey40",
  ylim = c(-8, 8),
  line_size = 0.7,
  point_size = 2.2,
  x_pad = 0.05,
  ...
)
}
\arguments{
\item{x}{An \code{easytsa} object from \code{\link[=tsa_create]{tsa_create()}}.}

\item{title}{Plot title (default: the analysis title).}

\item{subtitle}{Subtitle; default is an automatic line with RIS, effect
and diversity. Use \code{NULL} to suppress.}

\item{xlab, ylab}{Axis labels.}

\item{show_futility}{Draw the inner wedge (default: when computed).}

\item{show_conventional}{Draw the +/- 1.96 lines (default \code{TRUE}).}

\item{show_labels}{Label each look with its study name (default
\code{FALSE}).}

\item{show_favours}{Print "Favours ..." labels on the y axis sides
(default \code{TRUE}).}

\item{ris_label}{Text for the RIS line; \code{NULL} to suppress. Default
\code{"RIS = <n>"}.}

\item{col_z, col_bound, col_futility, col_conv}{Colours.}

\item{ylim}{Limits of the y axis (default \code{c(-8, 8)}, as in TSA).}

\item{line_size, point_size}{Sizes.}

\item{x_pad}{Extra proportion of the x axis to the right of the RIS or of
the last trial (default 0.05).}

\item{...}{Ignored.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the TSA graph (cumulative Z-curve, monitoring boundaries, inner
wedge, conventional limits and RIS) as a \code{ggplot} object, so any layer or
theme can be added afterwards.
}
\examples{
m <- meta::metabin(event.e, n.e, event.c, n.c, data = atb_peecs,
                   studlab = paste(author, year), sm = "RR",
                   method.tau = "REML", random = TRUE, common = FALSE)
x <- tsa_create(m, rrr = 0.30, label.e = "ATB", label.c = "No ATB")
tsa_plot(x)
tsa_plot(x, show_labels = TRUE, col_z = "black") +
  ggplot2::theme_classic()
}
