% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{tsa_bounds}
\alias{tsa_bounds}
\title{Lan-DeMets significance boundaries}
\usage{
tsa_bounds(
  t,
  alpha = 0.05,
  sides = 2,
  spending = "obf",
  ngrid = 801,
  trunc = 8
)
}
\arguments{
\item{t}{Increasing vector of information fractions in \verb{(0, 1]}.}

\item{alpha}{Total two-sided type I error (default 0.05).}

\item{sides}{\code{2} (default) or \code{1}.}

\item{spending}{Spending function, see \code{\link[=tsa_spending]{tsa_spending()}}.}

\item{ngrid}{Grid size of the numerical integration (default 801, which
reproduces the TSA software to about two decimals).}

\item{trunc}{Truncation of the boundary (default 8, as in TSA).}
}
\value{
Numeric vector of boundary values (positive; the lower boundary
is its negative when \code{sides = 2}).
}
\description{
Computes the two-sided (symmetric) or one-sided monitoring boundaries on
the Z scale for looks at information fractions \code{t}, spending \code{alpha}
according to \code{spending}. Two-sided boundaries follow the Lan-DeMets
program convention (Reboussin et al. 2000, also used by the TSA software
and the ldbounds package): a one-sided recursion spending \code{alpha / 2},
mirrored. Four equally spaced looks at \code{alpha = 0.05} give
4.333, 2.963, 2.359, 2.014.
}
\examples{
tsa_bounds(c(0.25, 0.5, 0.75, 1))
}
