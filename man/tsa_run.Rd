% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{tsa_run}
\alias{tsa_run}
\alias{tsa_create}
\title{Run a Trial Sequential Analysis in the TSA program}
\usage{
tsa_run(
  m,
  rrr = NULL,
  control = NULL,
  intervention = NULL,
  alpha = 0.05,
  beta = 0.2,
  diversity = "estimate",
  futility = TRUE,
  model = c("random_dl", "random_sj", "random_bt", "fixed"),
  outcome = c("negative", "positive"),
  label.e = NULL,
  label.c = NULL,
  title = NULL,
  file = NULL,
  keep_file = !is.null(file),
  jar = NULL
)

tsa_create(m, ...)
}
\arguments{
\item{m}{A \code{metabin} object. Trials, events and totals are taken from it;
estimates are not (the program fits its own model).}

\item{rrr}{Anticipated relative risk reduction, e.g. \code{0.20}. Default
\code{NULL} selects the program's "Estimate" option (empirical effect from
the meta-analysed data).}

\item{control, intervention}{Anticipated event proportions of the two
arms; alternative to \code{rrr}. Both \code{NULL} by default.}

\item{alpha}{Two-sided type I error (default 0.05).}

\item{beta}{Type II error (default 0.20 = 80 percent power).}

\item{diversity}{\code{"estimate"} (default, the program's "Model Variance
Based" heterogeneity correction) or a number in \verb{[0, 1)} used as a
user-defined D-squared.}

\item{futility}{Logical, apply the inner wedge (beta-spending, default
\code{TRUE}).}

\item{model}{Program model for the pooled estimate and Z-curve:
\code{"random_dl"} (default, DerSimonian-Laird), \code{"random_sj"},
\code{"random_bt"}, \code{"fixed"}.}

\item{outcome}{\code{"negative"} (default: the event is a harm, a relative
risk below 1 favours the intervention) or \code{"positive"}.}

\item{label.e, label.c, title}{Labels used in the file, print and plot.}

\item{file}{Path for the \code{.TSA} file. Default: a temporary file, deleted
after the run unless \code{keep_file = TRUE}.}

\item{keep_file}{Keep the \code{.TSA} file (default \code{TRUE} when \code{file} is
given).}

\item{jar}{Path to \code{TSA.jar}; default from \code{\link[=tsa_jar]{tsa_jar()}}.}

\item{...}{Passed to \code{\link[=tsa_run]{tsa_run()}}.}
}
\value{
An object of class \code{"easytsa"}: \code{looks} (one row per trial in
program order: \code{n_cum}, \code{IF}, \code{z}, \code{upper}, \code{futility}), \code{bounds},
\code{ris}, \code{pooled}, \code{conclusion}, \code{adjusted_ci}, \code{settings}, \code{file},
\code{engine}. Use \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, \code{\link[=tsa_plot]{tsa_plot()}}.
}
\description{
Writes the trials and settings of a \code{\link[meta:metabin]{meta::metabin()}} object to a \code{.TSA}
file, loads that file into the Copenhagen Trial Unit TSA program (run
headlessly inside R, see \code{\link[=tsa_engine]{tsa_engine()}}), lets the program compute the
required information size, the cumulative Z-curve, the monitoring
boundaries, the inner wedge and the heterogeneity estimate, and returns
those numbers.
}
\examples{
\dontrun{
tsa_setup()   # once per machine
m <- meta::metabin(event.e, n.e, event.c, n.c, data = atb_peecs,
                   studlab = paste(author, year), sm = "RR")
x <- tsa_run(m, label.e = "ATB", label.c = "No ATB",
             title = "PEECS after ESD")
x
summary(x)
tsa_plot(x)
}
}
