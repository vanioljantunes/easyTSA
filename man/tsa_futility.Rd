% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundaries.R
\name{tsa_futility}
\alias{tsa_futility}
\title{Lan-DeMets futility boundaries (inner wedge)}
\usage{
tsa_futility(
  t,
  upper,
  alpha = 0.05,
  beta = 0.2,
  spending = "obf",
  ngrid = 801,
  trunc = 8
)
}
\arguments{
\item{t}{Increasing vector of information fractions in \verb{(0, 1]}.}

\item{upper}{Significance boundaries at the same looks (from
\code{\link[=tsa_bounds]{tsa_bounds()}}).}

\item{alpha}{Two-sided type I error used for \code{upper}.}

\item{beta}{Type II error (1 - power).}

\item{spending}{Beta-spending function, see \code{\link[=tsa_spending]{tsa_spending()}}.}

\item{ngrid, trunc}{As in \code{\link[=tsa_bounds]{tsa_bounds()}}.}
}
\value{
Numeric vector of futility boundaries on the Z scale. Values are
\code{NA} where the boundary is not defined (negative, i.e. before the wedge
emerges) and equal \code{upper} where the two boundaries meet.
}
\description{
Computes the non-superiority (futility) boundaries for the positive side of
a two-sided design, spending \code{beta} with a beta-spending function under
the alternative hypothesis that the true drift equals the anticipated
effect. The inner wedge shown by the TSA software mirrors this boundary
to the negative side (non-inferiority).
}
\examples{
t <- c(0.25, 0.5, 0.75, 1)
tsa_futility(t, tsa_bounds(t))
}
