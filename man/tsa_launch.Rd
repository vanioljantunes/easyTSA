% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch.R
\name{tsa_launch}
\alias{tsa_launch}
\title{Launch the TSA software}
\usage{
tsa_launch(
  x,
  file = tempfile(fileext = ".TSA"),
  java = NULL,
  jar = NULL,
  wait = FALSE
)
}
\arguments{
\item{x}{An \code{easytsa} object or the path to an existing \code{.TSA} file.}

\item{file}{Where to write the \code{.TSA} file when \code{x} is an object
(default: a temporary file).}

\item{java}{Java executable (default: \code{JAVA_HOME/bin/java} if set,
otherwise \code{java} on the PATH).}

\item{jar}{Path to \code{TSA.jar}; default from \code{\link[=tsa_jar]{tsa_jar()}}.}

\item{wait}{Wait for the program to close (default \code{FALSE}).}
}
\value{
The \code{.TSA} path, invisibly. Errors when Java or the TSA program
is not found (the \code{.TSA} file is still written).
}
\description{
Writes the analysis to a \code{.TSA} file (when an \code{easytsa} object is given)
and starts the Copenhagen Trial Unit TSA program with Java (see
\code{\link[=tsa_jar]{tsa_jar()}} for where the program must be installed). The file is then
opened from inside the program (\code{File > Open}); the path is printed and
copied to the clipboard when possible.
}
\examples{
\dontrun{
m <- meta::metabin(event.e, n.e, event.c, n.c, data = atb_peecs,
                   studlab = paste(author, year), sm = "RR")
x <- tsa_create(m, rrr = 0.30)
tsa_launch(x, file = "peecs.TSA")
}
}
